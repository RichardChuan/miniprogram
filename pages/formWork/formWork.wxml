<block wx:for="{{menuItem}}" wx:key="index">
  <menu-item img="{{item.img}}" title="{{item.title}}" note="{{item.note}}" data-type="{{item.type}}" data-attr="{{item.attr}}" bind:onTap="onTap"></menu-item>
</block>