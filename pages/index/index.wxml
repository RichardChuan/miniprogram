<view class="banner">
  <view class="title">便捷高效 事半功倍</view>
  <view></view>
  <view class="sub_title">群防群治 齐心协力</view>
  <image class="image" src="/images/index.jpg" mode="aspectFill" lazy-load="false"></image>
</view>
<block wx:for="{{listItem}}" wx:key="index">
  <menu-item 
  img="{{item.img}}"
  title="{{item.title}}"
  note="{{item.note}}"
  data-type="{{item.type}}"
  data-attr="{{item.attr}}"
  bind:onTap="onTap"></menu-item>
</block>